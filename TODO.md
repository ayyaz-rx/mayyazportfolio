# TODO - Landing contact + color consistency

- [ ] Embed the full contact form section (from `src/app/contact/page.tsx`) into `src/app/page.tsx` as a dedicated section below Projects.
- [ ] Keep the existing hero mini contact CTA, but adjust it if needed so the landing page clearly shows the embedded form.
- [ ] Update `src/app/globals.css` so hardcoded background/text color usages are minimized and key background/fill colors derive from `--background` and `--text`.
- [ ] Verify there are no build/runtime errors with `npm run dev` (or `npm run build`).

